package config;

/**
 * Created by venkatesh on 5/7/17.
 */
public class EqSQLConfig {
    /* If rt.jar is on the java classpath, then leave this null.
     * Otherwise specify the path to rt.jar. */
    public static final String RT_JAR_PATH = null;
}
